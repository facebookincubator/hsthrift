diff --git a/thrift/compiler/CMakeLists.txt b/thrift/compiler/CMakeLists.txt
index 56d9c80f..c8a6ba9b 100644
--- a/thrift/compiler/CMakeLists.txt
+++ b/thrift/compiler/CMakeLists.txt
@@ -100,6 +100,7 @@ add_library(
   diagnostic.cc
   source_location.cc
 )
+set_target_properties(compiler_base PROPERTIES POSITION_INDEPENDENT_CODE "${BUILD_SHARED_LIBS}")
 target_link_libraries(
   compiler_base
   fmt::fmt
@@ -134,6 +135,7 @@ add_library(
   sema/sema_context.cc
   sema/resolution_mismatch.cc
 )
+set_target_properties(compiler_lib PROPERTIES POSITION_INDEPENDENT_CODE "${BUILD_SHARED_LIBS}")
 target_compile_definitions(compiler_lib PRIVATE THRIFT_OSS)
 target_link_libraries(
   compiler_lib
@@ -182,6 +184,7 @@ add_library(
   whisker/standard_library.cc
   whisker/token.cc
 )
+set_target_properties(whisker PROPERTIES POSITION_INDEPENDENT_CODE "${BUILD_SHARED_LIBS}")
 target_link_libraries(
   whisker
   fmt::fmt
diff --git a/thrift/lib/cpp/CMakeLists.txt b/thrift/lib/cpp/CMakeLists.txt
index ca04b077..4b45eac9 100644
--- a/thrift/lib/cpp/CMakeLists.txt
+++ b/thrift/lib/cpp/CMakeLists.txt
@@ -66,6 +66,8 @@ if (THRIFT_RPC)
     transport/TZlibTransport.cpp
     ../cpp2/transport/rocket/framing/Util.cpp
     ../cpp2/Flags.cpp
+    ../cpp2/transport/rocket/compression/CompressionAlgorithmSelector.cpp
+    ../cpp2/transport/rocket/compression/CompressionManager.cpp
     util/PausableTimer.cpp
     util/THttpParser.cpp
     util/VarintUtils.cpp
@@ -80,16 +82,6 @@ if (THRIFT_RPC)
       concurrency
       thrift-core
       rpcmetadata
-      # We need to link to libthriftcpp2.a after libtransport.a because
-      # libtransport.a uses
-      # apache::thrift::rocket::CompressionManager::uncompressBuffer()
-      # that is defined in libthriftcpp2.a.
-      #
-      # We don't need this for libtransport.so because libthriftcpp2.so
-      # and libtransport.so are cyclic dependencies and libthriftcpp2.so
-      # has libtransport.so dependency. We can't link to
-      # libthriftcpp2.so from libtransport.so. It's not allowed.
-      $<$<AND:$<NOT:$<BOOL:${BUILD_SHARED_LIBS}>>,$<BOOL:${THRIFT_RPC}>>:$<INSTALL_INTERFACE:FBThrift::thriftcpp2>>
       Folly::folly
       ZLIB::ZLIB
       ${OPENSSL_LIBRARIES}
diff --git a/thrift/lib/cpp2/CMakeLists.txt b/thrift/lib/cpp2/CMakeLists.txt
index 864a403e..537feebf 100644
--- a/thrift/lib/cpp2/CMakeLists.txt
+++ b/thrift/lib/cpp2/CMakeLists.txt
@@ -450,8 +450,6 @@ add_library(
   transport/rocket/FdSocket.cpp
   transport/rocket/ChecksumGenerator.cpp
   transport/rocket/Types.cpp
-  transport/rocket/compression/CompressionManager.cpp
-  transport/rocket/compression/CompressionAlgorithmSelector.cpp
   transport/rocket/compression/CustomCompressorRegistry.cpp
   transport/rocket/client/RequestContext.cpp
   transport/rocket/client/RequestContextQueue.cpp
